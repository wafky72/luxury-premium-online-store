{
  "name": "tori-crown-root",
  "private": true,
  "scripts": {
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "php artisan serve --prefix backend",
    "dev": "npx concurrently \"npm run dev --prefix frontend\" \"php artisan serve --working-dir backend\"",
    "install:all": "npm install --prefix frontend && composer install --working-dir backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
